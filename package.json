{
  "name": "gsdlabs-web",
  "version": "1.0.0",
  "description": "",
  "bin": {
    "gulp": "./node_modules/.bin/gulp"
  },
  "scripts": {
    "build": "./node_modules/.bin/gulp build",
    "start": "npm run build && node ./server/server.js",
    "dev": "./node_modules/.bin/nodemon -e html,js,scss --ignore client/dist/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git subtree push --prefix dist origin master"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.9.2",
    "nodemon": "^1.18.3"
  },
  "dependencies": {
    "del": "^3.0.0",
    "gulp": "^4.0.0",
    "gulp-autoprefixer": "^5.0.0",
    "gulp-clean-css": "^3.10.0",
    "gulp-cli": "^2.0.1",
    "gulp-concat": "^2.6.1",
    "gulp-minify": "^3.1.0",
    "gulp-nodemon": "^2.2.1",
    "gulp-sass": "^4.0.1",
    "gulp-uglify": "^3.0.1"
  }
}
